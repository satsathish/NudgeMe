apiVersion: v1
kind: Service
metadata:
  name: {{ include "nudgeme.fullname" . }}
  labels:
    {{- include "nudgeme.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nudgeme.selectorLabels" . | nindent 4 }}
